/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  env: {
    SANITY_PROJECT_ID: "gp7gjuwc",
  },

  // images: {
  //   domains: ["https://cdn.sanity.io/"],
  // },
};

module.exports = nextConfig;
